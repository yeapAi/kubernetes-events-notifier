apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes-events-notifier.fullname" . }}-test-connection"
  labels:
    {{- include "kubernetes-events-notifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubernetes-events-notifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
